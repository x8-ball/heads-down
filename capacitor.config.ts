import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "de.tf.headsdown",
  appName: "heads-down",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
